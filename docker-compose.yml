version: '3.7'

x-build-args: &build_args
  INSTALL_PYTHON_VERSION: 3.7

services:
  delayed-jobs-dev:
    build:
      context: .
      args:
        <<: *build_args
    image: chembl/delayed-jobs:dev
    ports:
    - "8080:8080"



